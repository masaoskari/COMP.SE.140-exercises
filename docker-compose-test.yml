name: test-container
services:
  test-container:
    build: ./tests
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    entrypoint: ["/bin/sh", "-c"]
    command: ["${TEST_COMMAND}"]
